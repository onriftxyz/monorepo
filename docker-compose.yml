version: "3.8"

services:
  db:
    image: postgres:latest
    ports:
      - 5555:5432
    restart: always
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: rift
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local
