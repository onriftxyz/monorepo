import { matter } from "~/components/fonts";
import { cn } from "~/lib/utils";

const Dashboard = () => {
  return (
    <main className={cn("bg-background min-h-screen", matter.className)}></main>
  );
};

export default Dashboard;
